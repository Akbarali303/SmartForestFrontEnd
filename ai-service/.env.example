# RTSP stream (to'liq URL yoki alohida)
# RTSP_URL=rtsp://admin:parol@192.168.0.61:554/cam/realmonitor?channel=1&subtype=0
RTSP_USER=admin
RTSP_PASSWORD=your_password
RTSP_HOST=192.168.0.61:554
RTSP_PATH=/cam/realmonitor?channel=1&subtype=0
# AI detection uchun past rezolyutsiya: subtype=1 (default), 0 = to'liq rez
# RTSP_SUBTYPE=1
# Kadr kengligi detection uchun (max 640 px barqarorlik uchun)
# DETECTION_MAX_WIDTH=640

# Telegram Bot API (BotFather token va chat id)
# POST https://api.telegram.org/bot<TOKEN>/sendPhoto
TELEGRAM_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id

# Xabar sarlavhasidagi kamera nomi (masalan: Cam1)
CAMERA_NAME=Cam1

# Odam yo'qolganidan keyin qancha sekund kutib yangi "paydo bo'ldi" hisoblansin (5–10, default 7)
NO_PERSON_RESET_SECONDS=7

# YOLO confidence (0..1) — modelga beriladi
CONFIDENCE_THRESHOLD=0.5
# Faqat shundan yuqori confidence hisobga olinadi (false positive kamaytirish, default 0.85)
# MIN_CONFIDENCE=0.85
# Kichik bbox (refleksiya/ob'ekt) filtri — detection kadrida min px
# MIN_BOX_WIDTH=25
# MIN_BOX_HEIGHT=50

# Kadrlar orasidagi interval (sekund) — tezlik uchun har N-kadrni tekshirish
FRAME_SKIP=5

# Telegram snapshot: JPEG sifati 95–100 (yuqori sifati)
# JPEG_QUALITY=95
# Crop atrofidagi padding (0.10–0.20), yaxshi kadrlash
# CROP_PADDING=0.15
