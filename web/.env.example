# Backend URL — Next.js shu manzilga /map va /api so‘rovlarni proxy qiladi
# Default: http://127.0.0.1:9000 (localhost o‘rniga 127.0.0.1 Internal Server Error ni oldini oladi)
BACKEND_URL=http://127.0.0.1:9000

# Xarita iframe uchun — brauzer to‘g‘ridan-to‘g‘ri shu URL’dan yuklaydi (chunk’lar 404 bo‘lmasin)
# Dev da: http://127.0.0.1:9000 yoki http://localhost:9000
NEXT_PUBLIC_BACKEND_URL=http://127.0.0.1:9000

# RTSP kamera — FFmpeg skripti (npm run camera:cam1) 401 xatosi bo‘lmasligi uchun
# web/.env yarating va quyidagilarni to‘ldiring ( .env ni git ga commit qilmang )
# RTSP_USER=admin
# RTSP_PASSWORD=your_camera_password
